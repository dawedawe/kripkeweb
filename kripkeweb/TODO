- fix encoding issues in console output
- function to check if subset forms a complete graph
- check use of termAsLamType
- clusterToDigraphCliqueMeasure with different formula schemes 
- partitions with different formula schemes, correlation of crosslink counts
  with schemes?
- replace criteria of good centroid randomness with real entropy measure
